# nix_config

A NixOS flake managing multiple hosts from a single repository, with secrets handled via agenix-rekey.

See [`CLAUDE.md`](./CLAUDE.md) for details and [`INSTALL.md`](./INSTALL.md) for the install walkthrough.
